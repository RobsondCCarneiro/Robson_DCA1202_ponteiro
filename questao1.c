/*Ponteiros é quando a variável guarda o endereço ao invés do valor da variável,
muito útil para quando é necessário quando implementa um programa que precisa de muitas
funções, porque assim o valor continua salvo quando resgatar o valor em outra função que invocou a outra.
A engenharia usa programas que são complexos e que precisam de muitas funções, então os ponteiros são úteis nesses casos.*/
