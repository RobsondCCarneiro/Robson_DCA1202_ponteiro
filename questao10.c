//x for declarado como char?
/* com x+1 o valor do ponteiro (endereço de memória) será 4093, com x+2 o valor do
 ponteiro (endereço de memória) será 4094, com x+3 o valor do ponteiro (endereço
 de memória) é 4095. */

//x for declarado como int?
/* com x+1 o valor do ponteiro (endereço de memória) será 4094, com x+2 o valor do
 ponteiro (endereço de memória) será 4096, com x+3 o valor do ponteiro (endereço
 de memória) é 4098. */

//x for declarado como float?
/* com x+1 o valor do ponteiro (endereço de memória) será 4096, com x+2 o valor do
 ponteiro (endereço de memória) será 409A, com x+3 o valor do ponteiro (endereço
 de memória) é 409E */

//x for declarado como double?
/* com x+1 o valor do ponteiro (endereço de memória) será 409A, com x+2 o valor
 do ponteiro (endereço de memória) será 40A3, com x+3 o valor do ponteiro
 (endereço de memória) é 40AB. */
